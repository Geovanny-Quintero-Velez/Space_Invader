package model;

public class Alien extends Character {

	public Alien(String path, int x, int y, int lenght, int width, double deltaX, double deltaY) {
		super(path, x, y, lenght, width, deltaX, deltaY);
	}
	
	
}
